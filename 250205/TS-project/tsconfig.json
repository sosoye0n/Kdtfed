{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleDetection": "force",
    "skipLibCheck": true,
    "outDir": "dist",
    "strict": true
  },

  "include": ["src"]
}

// target : 버전 관리
// "ES5" , "ESNext"

// Outdir : dist폴더 생성
// > js파일 따로 dist 폴더에 생성

// skipLibCheck : 작년에 동장
// > module간에 독립성 보장해서 오류 안남(hello,script에 같은 변수 설정해도)

// strict옵션 : 엄격한 true > any
// > message : string
// > 문제 해결
